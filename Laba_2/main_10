#!/bin/bash
echo 'Написать программу, находящую глубину вложенности, количество файлов, дату создания для всех подкаталогов начиная с заданного (первый аргумент командной строки). Вывести на консоль полный путь, размер, дату создания, права доступа, номер индексного дескриптора.'

find path_to_folder -maxdepth 3 -type f| wc -l